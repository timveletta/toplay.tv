enum GameStatus {
  LOBBY
  INGAME
  COMPLETE
}

enum GameType {
  TWOROOMSONEBOOM
}

type Game {
  code: String!
  createdAt: Int
  status: GameStatus!
  type: GameType!
}

type Query {
  getGame(code: String!): Game!
}

type Mutation {
  createGame: Game!
}
